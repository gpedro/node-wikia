/*global describe, it */
'use strict';
var assert = require('assert');
var Wikia = require('../');

describe('node-wikia node module', function () {
  it('to do', function () {
    assert(false, false);
  });
});
